# Regras do projeto

- Toda alteracao feita neste projeto deve ser commitada e pushada para o repositorio remoto no GitHub.
- Após qualquer modificacao de código, execute `git add`, `git commit` e `git push` antes de concluir a tarefa.